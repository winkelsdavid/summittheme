<!-- shipping-time.liquid -->
{%- if product.metafields.info.shipping != blank -%}
<div class="shipping-wrap p-4 border rounded">
  <span class="js-shipping-time" data-time="{{ product.metafields.info.shipping }}"></span>
  {%- if shipping_button != blank -%}
  <span data-toggle="modal" data-target="#jsShipping" class="link open-shipping ml-2">
    {{ shipping_button }} {%- render 'icon-truck' -%}
  </span>
  {%- assign pageShipping = pages[page_shipping] -%}
  <dialog class="modal" id="jsShipping">
    <div class="modal-dialog modal-dialog--shipping modal-dialog-centered" role="document">
      <div class="modal-content">
        <div class="modal-header">
          <button type="button" class="close" data-dismiss="modal">{%- render 'icon-close' -%}</button>
        </div>
        <div class="modal-body p-4">
          <h4 class="text-center">{{ pageShipping.title }}</h4>
          <div class=" justify-content-center js-shipping-content px-2 pb-1">
            {{ pageShipping.content }}
          </div>
        </div>
      </div>
    </div>
  </dialog>
  {%- endif -%}
</div>
<style>
  .modal-dialog--shipping img{width:100%}@media (min-width: 992px){.modal-dialog--shipping {max-width:900px}}.open-shipping {cursor: pointer;text-decoration: underline;}.shipping-wrap .icon-truck{margin:0 4px;    fill: none;
    stroke: currentColor;
    width: 24px;
    height: 24px;}
  /* native <dialog> centering/sizing for #jsShipping (replaces Bootstrap vendor geometry) */
  dialog#jsShipping.modal{max-width:100%;max-height:100%;height:100%;}
  dialog#jsShipping.modal .modal-dialog{display:flex;align-items:center;min-height:100%;width:100%;max-width:500px;margin:1.75rem auto;}
  dialog#jsShipping.modal .modal-content{width:100%;}
  @media (min-width: 992px){dialog#jsShipping.modal .modal-dialog{max-width:900px;}}
</style>
{%- endif -%}