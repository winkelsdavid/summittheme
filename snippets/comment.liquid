<!-- /snippets/comment.liquid -->
<div class="media">
  <div class="comment__user mr-4 rounded-circle justify-content-center align-items-center d-flex text-body">
    <img class="comment__user__gravatar rounded-circle" width="100" height="100" src="//www.gravatar.com/avatar/{{ comment.email | md5 }}?s=100" alt="{{ comment.author }}">
  </div>
  <div class="media-body">
    <p>
      <strong>{{ comment.author }}</strong>  
      <small class="txt-body-60">— {{ comment.created_at | time_tag: format: 'month_day_year' }}</small>
    </p>
    <div class="comment__content rte">
      {{ comment.content }}
    </div>
  </div>
</div>
