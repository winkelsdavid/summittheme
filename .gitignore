# Build artifacts / theme backups (the repo holds the SOURCE folders, not zips)
*.zip

# Local temp/debug files
_*
*.log
livetheme/
theme-import-safe/

# Node dev-tool dependencies (local checker only, not part of the live theme)
node_modules/

# Lokale Secrets (Shopify Admin-API-Token) - NIE committen
.env
.env.*

# Claude Code local config
.claude/

# OS junk
.DS_Store
Thumbs.db
desktop.ini
